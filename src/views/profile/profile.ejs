<%- include('../partials/header.ejs', {
        title : "User Profile",
        is_connect : true,
        is_display_buttons : true,
        script : "/profile.js",
        
        stylesheet_html: 
        '<link rel="stylesheet" href="/css-styles/style.css">' +
        '<link rel="stylesheet" href="/css-styles/profile.css">'
    }) 
%>
    <h1 id = "title">User Profile</h1>

    <div id = "personal-info">
        <h2>Personal Informations</h2>
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>First Name:</strong> <%= user.firstname %></p>
        <p><strong>Last Name:</strong> <%= user.lastname %></p>
    </div>

    <div>
        <h2>Games you want to play</h2>
        <ul>
            <% if (games && games.length > 0) { %>
                <% games.forEach(game => { %>
                    <li>
                        <strong><%= game.name %></strong><br>
                        <img src="<%= game.image %>" alt="<%= game.name %>" style="width: 50px; height: 50px;"><br>
                        <%= game.description %>
                    </li>
                <% }); %>
            <% } else { %>
                <li>No game selected.</li>
            <% } %>
        </ul>
    </div>

    <a href="/" class = "btn btn-sign">Back to home</a>

<%- include('../partials/footer.ejs') %>