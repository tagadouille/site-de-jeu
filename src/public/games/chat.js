$(document).ready(function () {

    // When the send message button clicked
    $("#send").click(function () { 
        
        // Recuperation of the message
        const message = $("#message").val().trim();

        if (message.length === 0) {
            return;
        }

        // Recuperation of the user_id

        // Sending the message :


        // Adding the message to the chat
    });

    // Receiving message :

});